#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ld long double
#define ull unsigned ll
#define x first
#define y second
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()
#define sz(a) ((int)a.size())

#define forn(i, n) for (int i = 0; i < n; i++)
#define rforn(i, n) for (ll i = n-1; i >= 0; i--)
#define aforn(v, a) for (auto& v : a)
#define print_float cout << fixed << setprecision(10)
#define print(_v) for(int i=0;i<(_v).size();++i){cout<<(_v)[i]<<' ';}cout<<'\n';

const ll mod = 1e9 + 7;
const ll modp = 998244353;

/*struct point {
    ll x, y, z;
};
bool cmp(point a, point b) {
  return tie(a.x, a.y, a.z) < tie(b.x, b.y, b.z);
}*/
// ll bpow(ll n, ll a) { return n == 0 ? 1 : n & 1 ? a * bpow(n - 1, a) % mod : bpow(n >> 1, a * a % mod); }

void solve() {
    
} 
 
int main() {
	ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	int t = 1;
	cin >> t;
    //print_float;
	while (t--) solve();    
	return 0;
}